{
  "name": "reason-react-native-web-example",
  "version": "0.6.3",
  "license": "MIT",
  "scripts": {
    "dev": "npm run reason:build && razzle start",
    "reason:watch": "bsb -make-world -w",
    "reason:clean": "bsb -clean-world",
    "reason:build": "bsb -make-world",
    "build": "rm -rf build && npm run reason:clean && npm run reason:build && razzle build",
    "start": "NODE_ENV=production node build/server.js"
  },
  "dependencies": {
    "bs-react-native": "0.10.0",
    "compression": "^1.6.2",
    "express": "^4.15.2",
    "helmet": "^3.6.1",
    "hpp": "^0.2.2",
    "morgan": "^1.9.1",
    "react": "16.8.4",
    "react-art": "16.8.4",
    "react-dom": "16.8.4",
    "react-native-web": "^0.9.0",
    "reason-react": "0.6.0"
  },
  "devDependencies": {
    "bs-platform": "4.0.18",
    "razzle": "^2.4.0"
  }
}
